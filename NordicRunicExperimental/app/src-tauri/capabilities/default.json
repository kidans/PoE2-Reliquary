{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the isolated Reliquary runic experiment.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
